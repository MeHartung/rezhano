{% for contactPhone in contactPhones %}
    <div class="contact-item">
        <a class="contact-item" href="callto:{{ contactPhone.phone }}"
            >{{ contactPhone.phone }}<span class="contact-item-separator">/</span>{{ contactPhone.name }}</a>
    </div>
{% endfor %}